import 'package:flutter/material.dart';

class AppColors {
  // Brand — K Ride Orange
  static const Color primary      = Color(0xFFFF6B00); // Orange
  static const Color primaryDark  = Color(0xFFE55A00); // Dark Orange
  static const Color primaryLight = Color(0xFFFFF3E8); // Light Orange bg

  // Driver brand — same orange for K Ride
  static const Color driverColor  = Color(0xFFFF6B00);

  // Backgrounds
  static const Color background   = Color(0xFFFFFFFF);
  static const Color surface      = Color(0xFFFFFFFF);
  static const Color cardBg       = Color(0xFFFFFFFF);
  static const Color gray         = Color(0xFFF6F6F6);

  // Text
  static const Color textPrimary  = Color(0xFF1A1A1A);
  static const Color textSecondary= Color(0xFF9E9E9E);
  static const Color textHint     = Color(0xFFBDBDBD);

  // Status
  static const Color success      = Color(0xFF2E7D32);
  static const Color error        = Color(0xFFE53935);
  static const Color warning      = Color(0xFFF59E0B);
  static const Color info         = Color(0xFF0277BD);

  // Misc
  static const Color divider      = Color(0xFFEEEEEE);
  static const Color shadow       = Color(0x14000000);
  static const Color black        = Color(0xFF000000);
  static const Color white        = Color(0xFFFFFFFF);

  // Service accent colours
  static const Color purpleAccent = Color(0xFF5E35B1);
  static const Color blueAccent   = Color(0xFF0277BD);
}
